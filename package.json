{
    "name": "claude-state-bar",
    "displayName": "Claude State Bar",
    "description": "Claude Code context usage + Claude.ai plan usage (5-hour session & weekly) in your VS Code status bar — with a live Workflow/Agent viewer, sound alerts, Remote-SSH support, and Telegram reset alerts.",
    "version": "1.7.29",
    "extensionKind": ["ui"],
    "publisher": "blueming",
    "author": {
        "name": "Blueming"
    },
    "contributors": [
        {
            "name": "Ed Zisk",
            "url": "https://github.com/ezoosk"
        }
    ],
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "https://github.com/comonetso/claudeStateBar"
    },
    "homepage": "https://github.com/comonetso/claudeStateBar#readme",
    "bugs": {
        "url": "https://github.com/comonetso/claudeStateBar/issues"
    },
    "icon": "images/icon.png",
    "galleryBanner": {
        "color": "#1a1a2e",
        "theme": "dark"
    },
    "keywords": [
        "claude",
        "claude code",
        "anthropic",
        "context window",
        "token counter",
        "ai assistant"
    ],
    "engines": {
        "vscode": "^1.74.0"
    },
    "categories": [
        "Other",
        "Visualization"
    ],
    "activationEvents": [
        "onStartupFinished"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "claudeContextBar.showSessionMenu",
                "title": "claudeStateBar: Session Menu"
            },
            {
                "command": "claudeContextBar.hideSession",
                "title": "claudeStateBar: Hide Session"
            },
            {
                "command": "claudeContextBar.restoreAllHidden",
                "title": "claudeStateBar: Restore All Hidden Sessions"
            },
            {
                "command": "claudeContextBar.restoreHiddenSession",
                "title": "claudeStateBar: Restore Hidden Session"
            },
            {
                "command": "claudeContextBar.showDiagnostics",
                "title": "claudeStateBar: Show Diagnostics"
            },
            {
                "command": "claudeContextBar.openSettings",
                "title": "claudeStateBar: Open Settings Panel"
            },
            {
                "command": "claudeContextBar.refreshPlanUsage",
                "title": "claudeStateBar: Refresh Plan Usage"
            },
            {
                "command": "claudeContextBar.testBeep",
                "title": "claudeStateBar: Test Beep Sound"
            },
            {
                "command": "claudeContextBar.cleanupOldVersions",
                "title": "claudeStateBar: Cleanup Old Extension Versions"
            },
            {
                "command": "claudeContextBar.cleanupGhostItems",
                "title": "Claude State Bar: Clean Up Ghost/Stale Status Bar Items"
            }
        ],
        "menus": {
            "commandPalette": [
                {
                    "command": "claudeContextBar.showSessionMenu",
                    "when": "false"
                },
                {
                    "command": "claudeContextBar.hideSession",
                    "when": "false"
                },
                {
                    "command": "claudeContextBar.restoreHiddenSession",
                    "when": "false"
                }
            ]
        },
        "configuration": {
            "title": "claudeStateBar",
            "properties": {
                "claudeState.language": {
                    "type": "string",
                    "enum": [
                        "en",
                        "ko"
                    ],
                    "enumDescriptions": [
                        "English",
                        "한국어"
                    ],
                    "default": "en",
                    "order": 0,
                    "description": "Language for the settings panel UI."
                },
                "claudeState.orgId": {
                    "type": "string",
                    "default": "",
                    "order": 2,
                    "description": "Claude.ai Organization ID (UUID). Found in claude.ai Network tab under /api/organizations/{UUID}/... Set this together with the Session Key via 'claudeStateBar: Open Settings Panel'. When empty, the status bar shows a setup warning."
                },
                "claudeState.refreshIntervalSec": {
                    "type": "number",
                    "default": 300,
                    "minimum": 10,
                    "maximum": 3600,
                    "order": 3,
                    "description": "Plan usage refresh interval in seconds (10–3600). Default: 300 (5 min). Session Key and Telegram Bot Token are stored encrypted via the settings panel, not here."
                },
                "claudeContextBar.autoColor": {
                    "type": "boolean",
                    "default": true,
                    "description": "Automatically assign different colors to each project (rainbow). When off, uses your base color with subtle variations."
                },
                "claudeContextBar.baseColor": {
                    "type": "string",
                    "enum": [
                        "White",
                        "Blue",
                        "Purple",
                        "Cyan",
                        "Green",
                        "Yellow",
                        "Orange",
                        "Pink"
                    ],
                    "default": "White",
                    "description": "Base color when Auto Color is off. Each project gets a subtle variation of this color."
                },
                "claudeContextBar.contextLimitDefault": {
                    "type": "number",
                    "default": 200000,
                    "description": "Context window size (tokens) for standard 200K models — Sonnet, Haiku, etc."
                },
                "claudeContextBar.contextLimitOpus": {
                    "type": "number",
                    "default": 1000000,
                    "description": "Context window size (tokens) for 1M-context models — Opus 4.x and any model with '1m' in its id."
                },
                "claudeContextBar.warningThreshold": {
                    "type": "number",
                    "default": 50,
                    "order": 11,
                    "description": "Percentage at which to show warning color (yellow)"
                },
                "claudeContextBar.dangerThreshold": {
                    "type": "number",
                    "default": 75,
                    "order": 12,
                    "description": "Percentage at which to show danger color (red)"
                },
                "claudeContextBar.refreshInterval": {
                    "type": "number",
                    "default": 30,
                    "description": "Refresh interval in seconds"
                },
                "claudeContextBar.idleTimeout": {
                    "type": "number",
                    "default": 180,
                    "minimum": 10,
                    "maximum": 7200,
                    "description": "Seconds of inactivity before a session is dimmed (grayscale) to indicate idle state. Default: 180 (3 minutes). The session stays visible until 'hideAfter' elapses."
                },
                "claudeContextBar.hideAfter": {
                    "type": "number",
                    "default": 86400,
                    "minimum": 60,
                    "maximum": 604800,
                    "description": "Seconds of inactivity before a session is fully hidden from the status bar. Default: 86400 (24 hours). Must be ≥ idleTimeout."
                },
                "claudeContextBar.scope": {
                    "type": "string",
                    "enum": ["workspace", "all"],
                    "enumDescriptions": [
                        "Only show Claude sessions for the current VS Code workspace folder(s).",
                        "Show all Claude sessions found in ~/.claude/projects/ regardless of which project is open."
                    ],
                    "default": "workspace",
                    "description": "Which Claude sessions to display. 'workspace' (default) shows only sessions for the currently open folder."
                },
                "claudeContextBar.showModel": {
                    "type": "boolean",
                    "default": true,
                    "description": "Show the model name (e.g., Sonnet 4.5) next to the percentage in the status bar. Takes extra space but tells you which model the session is using."
                },
                "claudeContextBar.autoCleanupOldVersions": {
                    "type": "boolean",
                    "default": true,
                    "description": "On activate, silently delete older versions of any installed VS Code extension (keeping only the highest semver per publisher.name). Prevents stale extension folders piling up after repeated vsix installs. Set to false to disable."
                },
                "claudeContextBar.compactMode": {
                    "type": "boolean",
                    "default": false,
                    "description": "Shorten project names to save status bar space. Multi-word names become acronyms (my-cool-project → MCP), single words become abbreviated (typescript → Tscript). Names ≤5 chars stay unchanged."
                },
                "claudeContextBar.shortNames": {
                    "type": "object",
                    "default": {},
                    "description": "Custom short names for projects. Keys are project names, values are the short versions (e.g. {\"my-cool-project\": \"MCP\", \"webapp\": \"WA\"})."
                },
                "claudeContextBar.soundWarning": {
                    "type": "string",
                    "default": "",
                    "description": "Path to the WAV file played once when context reaches the warning threshold. Leave empty to use the bundled default."
                },
                "claudeContextBar.soundDanger": {
                    "type": "string",
                    "default": "",
                    "description": "Path to the WAV file played twice when context reaches the danger threshold. Leave empty to use the bundled default."
                },
                "claudeContextBar.soundCompletion": {
                    "type": "string",
                    "default": "",
                    "description": "Path to the WAV file played when Claude finishes a response (stop_reason = end_turn). Leave empty to use the bundled default."
                },
                "claudeContextBar.soundWarningGain": {
                    "type": "number",
                    "default": 100,
                    "minimum": 50,
                    "maximum": 5000,
                    "description": "Warning sound volume gain in percent (50–5000). 100 = original WAV volume. Values above 100 amplify samples in-memory above the Windows master volume; values below 100 attenuate. WARNING: gains above ~300 typically cause clipping/distortion — the sound becomes harsher rather than louder. MP3 files only support attenuation (gain ≤ 100)."
                },
                "claudeContextBar.soundDangerGain": {
                    "type": "number",
                    "default": 100,
                    "minimum": 50,
                    "maximum": 5000,
                    "description": "Danger sound volume gain in percent (50–5000). See soundWarningGain for details."
                },
                "claudeContextBar.soundCompletionGain": {
                    "type": "number",
                    "default": 100,
                    "minimum": 50,
                    "maximum": 5000,
                    "description": "Completion sound volume gain in percent (50–5000). Useful when watching videos: raise this to hear the completion beep above the video's audio without raising the system master volume. WARNING: gains above ~300 cause clipping."
                },
                "claudeContextBar.completionBeepSettleMs": {
                    "type": "number",
                    "default": 3000,
                    "minimum": 100,
                    "maximum": 5000,
                    "description": "Settle window (ms) before firing the completion beep. After detecting end_turn, wait this long; if new activity appears (hook follow-up, auto-injected user message, or sub-agent continuation), suppress the beep. Default: 3000. Range: 100–5000."
                },
                "claudeContextBar.soundQuestion": {
                    "type": "string",
                    "default": "",
                    "description": "Path to the WAV/MP3 file played when Claude pauses to ask a question (AskUserQuestion or ExitPlanMode tool). Leave empty for the OS default (Speech On.wav on Windows)."
                },
                "claudeContextBar.soundQuestionGain": {
                    "type": "number",
                    "default": 100,
                    "minimum": 50,
                    "maximum": 5000,
                    "description": "Question sound volume gain in percent (50–5000). See soundWarningGain for details."
                },
                "claudeContextBar.soundWorkflow": {
                    "type": "string",
                    "default": "",
                    "description": "Path to the WAV file played when all workflow/task-agent sub-agents complete. Leave empty for the bundled default."
                },
                "claudeContextBar.soundWorkflowGain": {
                    "type": "number",
                    "default": 100,
                    "minimum": 50,
                    "maximum": 5000,
                    "description": "Workflow complete sound volume gain in percent (50–5000). See soundWarningGain for details."
                },
                "claudeContextBar.workflowCompleteBeep": {
                    "type": "boolean",
                    "default": true,
                    "description": "Play a beep sound when all workflow/task-agent sub-agents complete."
                },
                "claudeContextBar.detectStuckToolUse": {
                    "type": "boolean",
                    "default": false,
                    "description": "Heuristic: if the latest assistant entry is a tool_use (e.g., Bash, Edit) and no new activity has appeared for stuckToolUseThresholdSec, treat it as a likely VS Code permission prompt and fire the question beep. WARNING: this is a guess — legitimate long-running tools (builds, large file edits) will also trigger false positives. Off by default."
                },
                "claudeContextBar.stuckToolUseThresholdSec": {
                    "type": "number",
                    "default": 90,
                    "minimum": 30,
                    "maximum": 600,
                    "description": "Seconds of tool_use silence before the stuck-tool-use heuristic fires the question beep (only when detectStuckToolUse is on). Default: 90."
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "lint": "eslint src --ext ts",
        "package": "vsce package",
        "publish": "vsce publish"
    },
    "devDependencies": {
        "@types/node": "^18.0.0",
        "@types/vscode": "^1.74.0",
        "typescript": "^5.0.0"
    }
}