---
name: codex_rescue
description: Codex CLI 를 직접 호출해 2차 시선을 받는다. 세 모드를 인자의 **뜻으로** 가른다 — CONSULT(막힌 문제를 요청서로 정리해 가설을 반박받기, 기본) · REVIEW(바뀐 코드를 `codex exec review` 로 리뷰, git diff 기반) · EDIT(Codex가 직접 코드 수정, 확인 후 진행). 전 과정 자동이라 붙여넣기나 "다 됐다" 알림이 필요 없다. Codex는 자문역이며 응답 파일 외의 변경은 `send.sh` 가 실측해 강제 보고한다. "리뷰시켜", "리뷰해줘", "바뀐 코드 봐줘", "커밋 전에 훑어줘", "검토요청해", "분석해줘", "분석요청해", "검토및 수정요청해", "분석하고 수정시켜", "수정시켜", "고쳐줘", "코덱스에 물어봐", "코덱스 질문지", "codex_rescue", "코덱스 사용법" 등에 발동. `help`면 사용법만, `<request 경로>`면 그 건을 재실행하거나 도착한 응답을 검토한다.
---

# codex_rescue — Codex 2차 시선 자동 왕복

## 0. 의도 판정 — 다른 무엇보다 먼저 한다

발동하면 인자를 **맥락으로 읽어** 아래 5갈래 중 하나로 간다.

🔴 **키워드를 기계적으로 매칭하지 마라.** 사용자는 STT(음성 입력)로 말하므로 표현이 매번 다르다.
"리뷰시켜" · "리뷰해줘" · "바뀐 코드 봐줘" · "커밋하기 전에 훑어줘" 는 전부 같은 뜻이다.
아래 예시는 **판정 기준의 예시일 뿐 고정 목록이 아니다.**

| 사용자가 원하는 것 | 모드 | 이런 식으로 말한다 |
|---|---|---|
| 사용법 | **HELP** | `help` · `--help` · `-h` · `?` · `사용법` |
| 이미 만든 건 다시 굴리기 | **RESUME** | 인자 **전체**가 `.md` 경로 하나일 때만 |
| **바뀐 코드를 봐달라** | **REVIEW** | "리뷰시켜" · "리뷰해줘" · "바뀐 거 검토해" · "커밋 전에 훑어줘" |
| **보고 고쳐달라** | **EDIT** | "검토및 수정요청해" · "분석하고 수정시켜" · "수정시켜" · "고쳐줘" · "네가 고쳐" |
| 내가 막힌 걸 봐달라 | **CONSULT** (기본) | "검토요청해" · "분석해줘" · "분석요청해" · 문제 텍스트 · **인자 없음** |

**"분석"** 은 기본적으로 CONSULT 다 (Claude 가 막힌 문제를 분석받는 것). 단 "이 코드 분석해줘"
처럼 **대상이 작업 결과물**이면 REVIEW 다. 뒤에 "수정"이 붙으면(`분석하고 수정시켜`) EDIT 다.

### 판정은 두 축으로 한다

1. **대상이 무엇인가** — 작업 결과물 전체(바뀐 코드)냐, Claude 가 막힌 특정 문제냐
2. **Codex 가 고치기를 원하는가** — 의견만이냐, 직접 수정이냐

```
대상=바뀐 코드,   수정 안 함  →  REVIEW    (codex exec review, git diff 기반)
대상=막힌 문제,   수정 안 함  →  CONSULT   (요청서 기반, 가설 반박)      ← 기본
대상=막힌 문제,   수정 함      →  EDIT      (요청서 기반 + Codex 가 고침)
```

### 애매할 때

- **REVIEW vs CONSULT** — 둘 다 읽기 전용이라 잘못 골라도 손해가 적다. 되묻지 말고
  대화 맥락으로 정한다. 방금까지 코드를 고치고 있었으면 REVIEW, 문제에 막혀 있었으면 CONSULT.
- **EDIT 이 섞였는지 애매하면 → 반드시 한 줄로 확인한다.** 코드를 실제로 고치는 동작이고
  되돌리기 비용이 크다. 맥락 판정은 오탐 여지가 있으므로 여기서만 예외적으로 되묻는다.
- **문제 설명에 그 단어가 들어간 경우** — "로그인 리뷰가 안 돼" · "수정이 안 먹혀" 처럼
  **서술형**이면 그건 모드 지시가 아니라 **문제 텍스트**다. CONSULT 로 간다.
- **"리뷰하고 고쳐줘"** — `codex exec review` 는 read-only 전용이라 Codex 가 못 고친다.
  **REVIEW 로 받아서 Claude 가 고친다.** 그게 이 스킬의 전제("작업자는 Claude")에도 맞다.
- 경로 판정이 애매하면(문제 설명에 `.md` 가 섞임) RESUME 으로 가지 않는다.

## help 출력 — HELP 모드면 이것만 낸다

```
codex_rescue — Claude가 막혔을 때 Codex에게 2차 시선을 받는 스킬 (전 과정 자동)

[무엇]  제(Claude)가 막힌 문제를 요청서 파일로 정리 → codex CLI 를 직접 호출
        → Codex가 읽고 분석해 response 파일 저장 → 제가 자동으로 받아 검토·적용
        ★ 붙여넣기도, "다 됐다"고 알려주실 것도 없습니다. 손 안 대셔도 됩니다.

[왕복]  1. Claude  → docs/codex_rescue/<스탬프>_request_<슬러그>.md  작성
        2. Claude  → send.sh 를 백그라운드로 실행 (codex exec 호출)
        3. Codex   → 요청서 읽고 분석 → <스탬프>_response_<슬러그>.md 저장
                     (저장이 막혀도 -o 로 최종 메시지를 회수해 스크립트가 대신 저장)
        4. Claude  → Codex 종료 시 자동으로 깨어나 response 읽고 검토 → 적용 판단 보고

        분석이 도는 동안 다른 작업을 계속 지시하실 수 있습니다.

[안전]  Codex는 코드를 고치지 않습니다 (--edit 제외). 제 맥락이 끊기기 때문입니다.
        다만 저장 권한이 있으므로, 응답 파일 외에 무엇이 바뀌었는지
        send.sh 가 파일시스템으로 실측해 제게 강제 보고합니다.
        전제를 깬 변경이 있으면 제가 사용자께 보고하고 지시를 받습니다 — 임의로 되돌리지 않습니다.

[핵심]  요청서에 "제 가설 + 이미 실패한 시도"가 반드시 들어갑니다.
        반박할 대상이 있어야 다른 시선이 나옵니다. 없으면 제가 걸어간 길을 그대로 되밟습니다.

[명령]  ★ 말씀하시면 됩니다. 정해진 키워드가 아니라 뜻으로 알아듣습니다.

  /codex_rescue                       ★ 가장 흔함. 지금 막힌 걸 제가 알아서 특정
  /codex_rescue 분석해줘 · 검토요청해   제 가설을 Codex가 반박          (코드 안 고침)
  /codex_rescue 리뷰시켜               바뀐 코드를 Codex가 리뷰        (코드 안 고침)
  /codex_rescue 리뷰시켜 인증 위주로    ↑ + 집중해서 볼 지점 지정
  /codex_rescue 분석하고 수정시켜       Codex가 직접 코드 수정          (확인 한 번 받습니다)
  /codex_rescue <request 경로>         응답 왔으면 검토, 안 왔으면 재실행
  /codex_rescue help                  이 도움말

[리뷰]  "리뷰시켜" 는 git diff 를 대상으로 합니다. **git 레포에서만 됩니다.**
        - 커밋 안 된 변경이 있으면 → 그것을 리뷰
        - 작업 트리가 깨끗하면      → 기본 브랜치(main 등)와 비교
        - 특정 기준을 원하면 말씀만: "main 기준으로 리뷰해" / "이 커밋만 리뷰해"

[파일]  docs/codex_rescue/  (현재 작업 디렉토리 기준. 없으면 자동 생성)
        260726_014119_request_mms-jar-encoding.md    ← Claude 작성 (검토·수정 요청)
        260726_014119_response_mms-jar-encoding.md   ← Codex 작성 (스탬프·슬러그 동일)
        260726_014119_review_auth-refactor.md        ← 코드 리뷰 결과 (요청서 없음)
        .log/260726_014119_events.jsonl              ← Codex 가 실제로 한 일 전체 기록

[환경변수]  CR_MODEL=<모델>     Codex 모델 지정      CR_SANDBOX=<모드>  권한 수준
            CR_ALLOW_EDIT=1     EDIT 해금(승인 후)   CR_DRYRUN=1        실행 안 하고 확인만
```

출력 후 한 줄 덧붙인다: "바로 물어보려면 `/codex_rescue` 만 치시면 됩니다."

## 목적 (설계의 전제 — 절대 흔들지 말 것)

**모든 작업은 Claude와 사용자가 한다. Codex는 작업자가 아니라 자문역이다.**

Claude가 해결하지 못하는 문제가 가끔 생긴다. 그럴 때 **다른 시선의 분석**을 받으려고 Codex를 부른다.
따라서 이 스킬의 산출물은 "Codex가 실행할 작업지시"가 아니라 **"Codex가 반박하고 다른 각도를 제시할 분석 요청서"** 다.

이 전제에서 따라 나오는 규칙:

- **Codex는 코드를 수정하지 않는다.** Codex가 고치면 Claude가 모르는 변경이 생기고 맥락이 끊긴다. (`--edit` 만 예외)
- **Codex가 쓰는 파일은 response 문서 하나뿐이다.** 그 외에는 아무것도 만들지 않는다.
- **그 전제를 프롬프트 지시에만 맡기지 않는다.** `send.sh` 가 실행 전후를 실측 비교해 응답 파일 외의 변경을 잡아내 Claude에게 강제 보고한다. (§ 변경 감지)
- **요청서에는 Claude의 현재 가설을 반드시 담는다.** 반박할 대상이 있어야 다른 시선이 나온다. 이게 없으면 Codex는 Claude가 이미 걸어간 길을 그대로 되밟는다.
- **응답의 최종 소비자는 Claude다.** 사용자에게 읽히는 글이 아니라, Claude가 곧바로 적용 판단할 수 있는 형태여야 한다.

## 왕복 구조 — 사람 손이 들어가지 않는다

```
Claude  ─►  docs/codex_rescue/260726_014119_request_<slug>.md   저장
Claude  ─►  Bash(run_in_background: true)
              bash ~/.claude/skills/codex_rescue/send.sh <request 경로>
Codex   ─►  요청서를 읽고 분석  ─►  260726_014119_response_<slug>.md  직접 저장
send.sh ─►  응답 회수 + 변경 실측  ─►  stdout 으로 Claude 에게 보고
Claude  ─►  ★ codex 종료 시 자동 재호출됨  →  response 읽고 검토  →  적용 판단 보고
```

**왜 백그라운드인가** — Claude Code 의 `run_in_background` 는 명령이 끝나면 **Claude를 자동으로 다시 깨우고 stdout을 넘긴다.** 그래서 hook을 등록하지 않아도 도착을 즉시 인지한다. 동기 호출은 Bash 타임아웃(최대 10분)에 걸릴 수 있고 대기 중 다른 작업을 못 한다. 백그라운드면 Codex가 분석하는 동안 사용자가 다른 일을 계속 지시할 수 있다.

**왜 파일로 주나** — 긴 요청서를 인자로 넘기면 따옴표·개행이 깨진다. 경로만 넘기고 Codex가 직접 읽는다. 그리고 요청·응답이 프로젝트에 기록으로 남아 git으로 추적된다.

**쓰기 실패로 왕복이 끊기지 않는다** — `send.sh` 는 `codex exec -o <파일>`(`--output-last-message`)로 Codex의 최종 메시지를 항상 파일로 받아둔다. Codex가 지정 경로에 저장하지 못했으면 스크립트가 그 메시지를 규약 형식으로 감싸 `response_path` 에 저장하고 `author: codex-via-stdout` 으로 표시한다. **사용자가 답변을 옮겨 붙일 일은 없다.**

## 변경 감지 — workspace-write 의 구멍을 막는다

Codex 는 `-s workspace-write` 로 실행된다(응답 파일을 직접 저장하려면 필요하다). 그러면 **워크스페이스 안의 다른 파일도 쓸 수 있다.** "Codex는 코드를 고치지 않는다"는 전제가 프롬프트 준수에만 의존하게 되므로, `send.sh` 가 파일시스템으로 실측한다.

- 실행 직전 **마커 파일**(시간 기준점)과 **전체 파일 경로 목록**을 남긴다
- 실행 후 마커보다 새로운 mtime = 생성·수정, 경로 목록 차집합 = 삭제
- 정상 산출물(`response_path`, `.log/`)을 뺀 나머지가 **"Codex가 몰래 만진 것"**
- 있으면 stdout에 🔴 로 보고된다. Claude는 그것을 **반드시 사용자에게 보고**한다
- 🔴 **임의로 되돌리지 않는다.** 되돌릴지 살릴지는 사용자 판단이다
- `.log/<스탬프>_events.jsonl` 에 Codex가 실행한 모든 이벤트가 남는다. 무엇을 했는지 따질 때 이걸 읽는다
  (포맷이 바뀌면 깨지므로 스크립트는 파싱하지 않고 보존만 한다)

성능 때문에 `.git`·`node_modules`·`build`·`.gradle`·`.dart_tool`·`.venv`·`.next`·`__pycache__` 는 스캔에서 제외한다. **그 안의 변경은 못 잡는다** — 의도된 절충이다.

## 산출물 규약

| 항목 | 규칙 |
|---|---|
| 디렉토리 | **현재 작업 디렉토리 기준 상대경로 `docs/codex_rescue/`**. 절대경로 하드코딩 금지(여러 프로젝트·머신에서 쓰인다). |
| 없으면 | `mkdir -p docs/codex_rescue` 로 **생성한다.** 물어보지 말고 만든다. |
| 스탬프 | `ymd_His` (예: `260726_014119`). **반드시 `date "+%y%m%d_%H%M%S"` 를 실행해 얻는다. 시각을 지어내지 마라.** |
| 슬러그 | 영문 kebab-case 2~4단어 (예: `mms-jar-encoding`, `fcm-token-null`). |
| 요청 파일 | `docs/codex_rescue/<스탬프>_request_<슬러그>.md` — **Claude가 작성** (CONSULT·EDIT 만) |
| 응답 파일 | `docs/codex_rescue/<스탬프>_response_<슬러그>.md` — **Codex가 작성**(저장 실패 시 send.sh 가 대신). request와 동일 스탬프·슬러그로 짝을 맞춘다. |
| 리뷰 파일 | `docs/codex_rescue/<스탬프>_review_<슬러그>.md` — **`send.sh` 가 작성.** REVIEW 는 요청서가 없고(대상이 git diff), Codex 도 파일을 쓰지 않으므로(read-only) `-o` 로 받은 결과를 스크립트가 규약 형식으로 저장한다. 스탬프도 `send.sh` 가 만든다 — 짝 맞출 요청서가 없기 때문이다. |
| 실행 기록 | `docs/codex_rescue/.log/<스탬프>_events.jsonl` (Codex 이벤트 전문) · `_last_message.md` (최종 메시지 원본) · `_stderr.log` |

**응답 파일명은 Claude가 정해서 요청서 frontmatter 의 `response_path` 에 박아 넣는다.** Codex에게 이름을 짓게 하면 스탬프를 지어내거나 슬러그가 어긋나 짝이 깨진다. 그리고 **`send.sh` 가 그 `response_path` 값을 읽어서 동작한다** — 이 필드가 없으면 스크립트가 즉시 멈춘다.

## 모드

| 모드 | Codex 가 하는 일 | 코드 수정 | 리뷰 대상 | 요청서 |
|---|---|---|---|---|
| **CONSULT** (기본) | Claude 의 가설을 반박하고 수정 방법을 제시 | ❌ | Claude 가 막힌 특정 문제 | 필요 |
| **REVIEW** | `codex exec review` 로 변경분을 리뷰 | ❌ (CLI 가 read-only 고정) | **git diff** | 불필요 |
| **EDIT** (비권장) | 직접 코드를 수정 | ✅ | 요청서가 지정한 파일 | 필요 |

> ⚠️ **EDIT 은 발동 전에 확인 한 줄을 받는다.** "Codex 가 직접 고칩니다 — 제가 모르는 변경이 생겨
> 맥락이 끊깁니다. 커밋 안 한 작업이 있으면 먼저 커밋하시길 권합니다. 진행할까요?"
> 맥락 판정은 오탐 여지가 있고 코드 수정은 되돌리기 비용이 크다. 작업 후 `git diff` 로 변경분을
> Claude 가 직접 확인해 response 파일에 기록한다.

> ### 🔴 EDIT 게이트 — 스크립트가 막는다 (2026-08-17)
>
> 위 "확인 한 줄"은 **규칙일 뿐이었고 강제력이 없었다.** Codex 의 2차 검토가 지적했고 코드로 확인했다 —
> `send.sh` 는 `mode: edit` 이면 그냥 실행했다. 즉 **안전장치가 Claude 의 기억력에 달려 있었다.**
>
> 이제 `send.sh` 가 **기본 거부**한다. 승인을 받은 뒤에만 환경변수를 붙여 실행한다.
>
> ```bash
> CR_ALLOW_EDIT=1 bash ~/.claude/skills/codex_rescue/send.sh <요청서>
> ```
>
> 🔴 **승인 없이 이 환경변수를 붙이지 마라.** 그러면 게이트를 만든 의미가 없다.
> 붙이는 행위 자체가 "사용자 승인을 받았다"는 표시다.

> **REVIEW 는 확인 없이 바로 돈다.** `codex exec review` 는 read-only 전용이라 위험이 없다.

## 이중 위임 — 하지 않는다

`remote_req` 로 서버 Claude 를 깨워서 `codex_rescue` 를 시키지 마라.
**결과가 반쪽만 온다** — 응답 파일은 서버에 남고 로컬은 원격 `claude -p` 의 stdout 만 회수한다.

대신 서버에 **사실을 묻고**, 받은 답을 재료로 **로컬에서** Codex 에 묻는다.

> ⚠️ 이건 **거의 일어나지 않는다** — 사용자 지적(2026-08-17)으로 정정한 판단이다.
> 서버 Claude 는 `remote_req` 헤더로 "물어본 것만 답한다·고치지 않는다"가 강제된 비대화형
> 세션이라 스스로 다른 스킬을 발동할 이유가 없다. 로컬 Claude 가 **명시적으로**
> "코덱스한테 물어봐"를 보내야 성립하는데 그럴 이유가 없다.
>
> **사용자가 서버에 직접 접속해 `codex_rescue` 를 쓰는 것은 금지가 아니다** — 그래서 4대에 배포했다.
> 금지 대상은 "자동 연쇄"뿐이다.

## 언제 어느 스킬을 쓰는가 (2026-08-17 추가)

`remote_req` 와 `codex_rescue` 는 **"요청"·"검토" 라는 말이 겹치는데 생명주기가 정반대**다.
Codex 가 지적한 혼동 지점이라 표로 고정한다.

| 하려는 것 | 쓸 것 | 상대를 깨우나 |
|---|---|---|
| 서버/앱 Claude 에게 **사실을 묻기** | `remote_req` QUERY | 깨운다 (답이 온다) |
| 상대 팀에 **수정 요청서 남기기** | `remote_req` REQUEST | **안 깨운다** (문서만 떨군다) |
| 내가 한 변경을 **알리고 영향만 받기** | `remote_req` NOTICE | 깨운다 |
| **내가 바꾼 코드**를 검토받기 | `codex_rescue` REVIEW | Codex 를 깨운다 |
| **내가 막힌 문제**의 가설을 반박받기 | `codex_rescue` CONSULT | Codex 를 깨운다 |
| Codex 에게 **직접 고치게** 하기 | `codex_rescue` EDIT | 깨운다 + **게이트** |

핵심 구분: **`remote_req` = 공간 축**(멀리 있는 Claude) · **`codex_rescue` = 관점 축**(다른 모델).

## REVIEW 절차 — "리뷰시켜" 로 발동

1. **git 레포인지 확인한다.** 아니면 실행하지 말고 알린다 — 리뷰는 git diff 를 대상으로 하므로
   git 레포에서만 된다. (`send.sh` 도 거부하지만, 미리 알리는 게 낫다)
   막힌 문제를 물어보려는 것이었으면 CONSULT 로 안내한다.

2. **슬러그를 정한다** — 영문 kebab-case 2~4단어. 무엇을 리뷰하는지 담는다 (`auth-refactor`, `mms-sender-fix`).
   요청서가 없으므로 이게 유일한 식별자다. 스탬프는 `send.sh` 가 만든다.

3. **스코프를 맥락에서 잡는다.**
   - "main 기준으로" · "브랜치 전체" → `--base main`
   - "이 커밋만" · 특정 SHA 언급 → `--commit <SHA>`
   - "지금 작업분" · "커밋 전에" → `--uncommitted`
   - 아무 말 없으면 **넘기지 않는다.** `send.sh` 가 자동 판정한다
     (커밋 안 된 변경이 있으면 그것을, 깨끗하면 기본 브랜치 대비)

4. **백그라운드로 실행한다.**

   ```
   Bash(run_in_background: true):
     bash ~/.claude/skills/codex_rescue/send.sh --review --slug <슬러그> [--base X|--commit Y] [집중지시]
   ```

5. **자동 재호출 → 검토.** 리뷰 결과는 `docs/codex_rescue/<스탬프>_review_<슬러그>.md` 에 저장된다.
   1. Read 한다
   2. **Codex 원문을 고치지 마라.** 끝에 `## Claude 검토` 를 덧붙인다 — 지적별로 채택/보류/기각 + 이유
   3. 🔴 **지적을 자동으로 전부 고치지 마라.** 리뷰에는 오탐과 취향 문제가 섞인다.
      오탐이라 판단하면 코드로 반증한 결과를 적는다. 무엇을 고칠지는 사용자 판단이다

### 🔴 codex CLI 제약 — 스코프와 집중 지시는 함께 못 쓴다

실측(2026-08-17): `codex exec review` 는 스코프 플래그와 `[PROMPT]` 가 **배타적**이다.

```
error: the argument '--uncommitted' cannot be used with '[PROMPT]'
```

`--base` · `--commit` 도 같다. 그래서 `send.sh` 는 이렇게 처리한다.

- **집중 지시가 없으면** → 스코프 플래그를 쓴다 (정확)
- **집중 지시가 있으면** → 플래그를 버리고 **스코프를 문장으로 녹여** 프롬프트에 넣는다
  (`"커밋되지 않은 변경을 리뷰 대상으로 삼아라.\n\n<집중지시>"`)

두 번째 경우는 플래그만큼 정확하지 않다. `send.sh` 보고에 어느 방식이었는지 나오고
응답 파일 frontmatter 에 `scope_via: flag|prompt` 로 기록된다. **prompt 방식이면 리뷰 본문에서
실제로 그 범위를 봤는지 확인해라.** 범위가 어긋났으면 집중 지시를 빼고 플래그 방식으로 다시 돌린다.

## 절차 — CONSULT / EDIT (요청서 기반. 발동하면 이 순서로)

1. **의도 확정** — § 0 의 5갈래 판정. HELP·RESUME·REVIEW 면 여기로 오지 않는다.
   Codex 가 직접 고치기를 원하는 뜻이면 EDIT(**확인 한 줄 받고 진행**), 아니면 CONSULT(기본).
   모드 지시어를 뺀 나머지 텍스트가 있으면 그게 집중할 문제다.

2. **대상 문제 특정** — ★ **문제 텍스트가 없는 발동이 기본이다.** 사용자에게 되묻지 말고 대화 맥락에서 Claude가 직접 특정한다. 무엇을 시도했고 어디서 막혔는지는 사용자보다 Claude의 이력에 더 정확히 남아 있다.

   특정 우선순위:
   1. **지금 막혀 있는 것** — 방금 실패한 시도, 해결 못 한 에러·증상
   2. **반복 실패 지점** — 두 번 이상 다른 방법으로 시도했는데 여전히 안 되는 것
   3. **사용자가 최근 답답함을 표한 지점** — "아직도 안 되네", "왜 이러지" 등
   4. 위가 모두 없으면 이번 세션의 미해결 과제 중 가장 최근 것

   후보가 둘 이상이면 **멈추지 말고** 가장 우선순위 높은 것으로 진행하되, 요청서를 낸 뒤 채팅에 한 줄로 밝힌다:
   "`<문제 A>` 건으로 작성했습니다. 다른 걸 원하시면 말씀해 주세요."
   막혀 있는 게 정말 아무것도 없으면 그때만 무엇을 물어볼지 되묻는다.

   이때 **Claude의 가설도 함께 자동으로 채운다.** 내가 원인을 무엇이라 보고 그래서 무엇을 시도했는지는 전부 내 이력이다. 사용자에게 묻지 말고 직접 쓴다.

3. **스탬프 획득** — `date "+%y%m%d_%H%M%S"` 를 실제로 실행한다. 추측 금지.

4. **디렉토리 확보** — `mkdir -p docs/codex_rescue`.

5. **재료 수집** — 2에서 특정한 문제에 대해 아래 4종을 대화 맥락에서 채운다:
   - **증상** — 무엇이 어떻게 어긋나는지 (구체적으로)
   - **Claude의 현재 가설과 근거** ← 이 스킬의 핵심. 반박 대상을 제공한다.
   - **이미 시도하고 실패한 것** ← Codex가 같은 제안을 반복하지 않게.
   - **환경** — 언어·프레임워크·버전·기기·툴 체인, 관련 파일 위치

6. **관련 코드를 실제로 Read해서 인용** — 추측 금지. Codex CLI 는 워크스페이스 파일을 직접 읽을 수 있지만, **워크스페이스 밖 경로는 못 읽을 수 있다.** 요청서 하나만으로 판단이 서게 자체완결적으로 쓴다.

7. **request 파일 Write** — frontmatter 의 **`response_path` 를 완성된 실제 경로로 박는다.** `send.sh` 가 이 값을 읽어 동작하므로 빠지면 스크립트가 멈춘다.

8. **`send.sh` 를 백그라운드로 실행** — 이게 전달이다. 사용자에게 붙여넣기를 시키지 않는다.

   ```
   Bash(run_in_background: true):
     bash ~/.claude/skills/codex_rescue/send.sh docs/codex_rescue/<스탬프>_request_<슬러그>.md
   ```

   - **반드시 `run_in_background: true`** 로 던진다. 동기로 부르면 Bash 타임아웃(최대 10분)에 걸리고 대기 중 다른 작업을 못 한다.
   - 던진 직후 채팅에는 짧게만 낸다 (§ 출력 형식). 사용자는 그동안 다른 작업을 지시할 수 있다.
   - 문제를 자동 특정했다면 어느 건으로 썼는지 한 줄 밝힌다.

9. **자동 재호출 → 검토** — Codex 가 끝나면 Claude 가 **자동으로 깨어나** `send.sh` 의 stdout 을 받는다. 사용자가 알려줄 필요가 없다. 받으면:

   1. 보고에 🔴 **응답 파일 외 변경**이 있으면 **그것을 가장 먼저 사용자에게 보고한다.** 스킬 전제가 깨진 것이므로 검토보다 우선한다. **임의로 되돌리지 마라** — 되돌릴지는 사용자 판단이다.
   2. `response_path` 를 Read 한다. **Codex 원문을 고치지 마라.** 파일 끝에 `## Claude 검토` 섹션을 덧붙인다.
   3. 검토 결과(채택 / 보류 / 기각 + 각각의 이유, 적용 계획)를 보고하고 적용 여부 판단을 받는다.
   4. 응답 파일이 없다고 나오면 `.log/<스탬프>_events.jsonl` 과 `_stderr.log` 를 읽어 원인을 사용자에게 보고한다.

## RESUME 모드 — 인자가 request 파일 경로일 때

요청서를 **새로 쓰지 않는다.** 이미 만든 건을 다시 굴리는 모드다.

1. 그 경로를 Read 한다. 없으면 `docs/codex_rescue/` 를 훑어 비슷한 이름을 제시하고 멈춘다.
2. frontmatter 의 `response_path` 를 읽는다.
3. **응답 파일이 이미 있으면** — Read 해서 검토하고 적용 판단을 보고한다. (§ 절차 9 와 동일)
4. **아직 없으면** — 그 건을 다시 실행한다 (§ 절차 8 과 동일). 실행 전 `.log/<스탬프>_stderr.log` 가 있으면 먼저 읽어 **지난 실패 원인을 파악하고 같은 실패를 반복하지 않는다.**
5. 요청서 내용을 채팅에 다시 뿌리지 않는다 — 파일이 정본이다.

## 템플릿 — readonly (기본)

request 파일에 아래 구조로 쓴다. (`<...>` 는 실제 값으로 치환 — **특히 응답 저장 경로는 완성된 실제 경로를 박는다**)

```markdown
---
type: codex_request
mode: readonly
stamp: <260726_014119>
slug: <mms-jar-encoding>
response_path: docs/codex_rescue/<260726_014119>_response_<mms-jar-encoding>.md
---

# Codex 요청 — <한 줄 제목>

## 이 요청의 성격
나(Claude)가 이 문제에서 막혔다. 너에게 **다른 시선의 분석**을 받고 싶다.
**코드는 절대 수정하지 마라.** 네가 쓸 파일은 아래 지정한 응답 문서 하나뿐이다.

## 답변을 남길 곳  ← 먼저 읽어라
분석이 끝나면 아래 경로에 **그 이름 그대로** 파일을 만들어 저장해라.

    docs/codex_rescue/<260726_014119>_response_<mms-jar-encoding>.md

- 경로·파일명을 바꾸지 마라. 스탬프와 슬러그는 이 요청서와 짝을 이룬다.
- 파일 첫머리에 아래 frontmatter를 그대로 넣어라:

      ---
      type: codex_response
      mode: readonly
      stamp: <260726_014119>
      slug: <mms-jar-encoding>
      author: codex
      ---

- **쓰기가 막히면 거기서 멈추지 말고, 같은 내용을 최종 메시지로 그대로 출력해라.** 자동으로 회수해 저장한다. 저장 실패를 이유로 분석을 생략하지 마라.
- **이 파일 외에는 아무것도 만들거나 수정하거나 삭제하지 마라.** 임시 파일·메모·테스트 파일도 금지다.
  (변경 여부는 파일시스템으로 실측 검증되며, 응답 파일 외의 변경은 전부 보고된다.)

## 환경
- 언어/프레임워크/버전, 기기, 빌드·툴 체인 (구체적으로)

## 문제
- 증상을 구체적으로. "안 된다"가 아니라 무엇이 어떻게 어긋나는지.

## 현재 코드
- 문제 지점의 실제 코드 인용(핵심 함수·호출부·시그니처). 파일 경로와 라인 표기.

## 내(Claude)가 세운 가설과 근거     ← 반드시 포함
- 나는 원인을 <X>라고 보고 있다. 근거: <...>
- 그래서 <Y>를 시도했다.
- **이 가설 자체가 틀렸을 수 있다. 먼저 이걸 의심해줘.**

## 시도했고 실패한 것     ← 반드시 포함
1) ... → 결과(효과 없음/에러/부분개선)
2) ... → 결과

## 요청 — 다른 시선을 원한다
1. **위 가설을 먼저 반박해봐.** 내가 놓친 전제나 잘못 읽은 지점이 있나?
2. 네가 보는 근본 원인은 무엇인가. 왜 지금 접근이 안 먹는지.
3. 이 버전 기준 정확한 수정 방법을 코드로(before/after) 제시해줘. 텍스트로만.
4. 대안이 여럿이면 장단점 비교 + 이 케이스 추천.
5. 관련 함정·주의점.
6. **확신도**를 밝혀줘. 추측인 부분과 확실한 부분을 구분해서.

## 답변 형식 — 이 답변은 Claude가 읽고 바로 적용 판단한다
응답 문서에 아래 순서로 써라.

1. `내 가설에 대한 판정` — 동의 / 부분 동의 / 기각 + 이유
2. `네가 보는 근본 원인`
3. `수정 방법 (before → after)`
4. `대안 비교와 추천`
5. `함정·주의점`
6. `확신도와 남은 불확실성` — 내가 추가로 확인해야 할 것
7. `추가로 필요한 자료` — 없으면 "없음"

## 추가 제공 가능
- 필요하면 <파일 X 전체 / 설정 / 로그>를 더 줄 수 있다. 무엇이 더 필요한지 7번에 적어줘.
```

## 템플릿 — --edit (비권장, 명시 요청 시에만)

```markdown
---
type: codex_request
mode: edit
stamp: <스탬프>
slug: <슬러그>
response_path: docs/codex_rescue/<스탬프>_response_<슬러그>.md
---

# Codex 작업 지시 — <한 줄 제목>

## 환경 / 문제 / 대상 파일 / 맥락 코드 / 시도했고 실패한 것
(readonly 템플릿과 동일하게 채운다)

## 작업 — 직접 수정할 것
1. 근본 원인을 진단하고 위 대상 파일을 직접 수정해줘.
2. **최소 변경**만. 문제와 무관한 리팩터·포맷 변경 금지.
3. 대상 파일 외에는 건드리지 마. 필요하면 먼저 물어봐.
4. 확신이 안 서면 수정 전에 물어봐.

## 보고 — 아래 경로에 저장할 것
    docs/codex_rescue/<스탬프>_response_<슬러그>.md
    (frontmatter: type: codex_response / mode: edit / stamp / slug / author: codex)
    쓰기가 막히면 같은 내용을 최종 메시지로 그대로 출력해라. 자동으로 회수해 저장한다.

1. 변경한 파일·라인 요약
2. 무엇을 왜 바꿨나 (before → after)
3. 빌드·검증 방법
4. 남은 리스크·확신도
```

edit 모드에서는 Claude가 작업 후 반드시 `git diff` 로 실제 변경분을 직접 확인한 뒤 response 파일에 함께 기록한다.

## 응답 파일 — 원문과 검토를 분리한다

Codex가 직접 저장한 파일이든 `send.sh` 가 최종 메시지로 대체 저장한 파일이든, 최종 형태는 같다.

```markdown
---
type: codex_response
mode: <readonly|edit>
stamp: <request와 동일>
slug: <request와 동일>
author: <codex | codex-via-stdout>
---

# Codex 응답 — <제목>

## Codex 원문
(받은 내용 그대로. 요약·윤색 금지 — 나중에 재검토할 원자료다.)

## Claude 검토     ← Claude가 나중에 덧붙이는 섹션
- 채택할 것 / 보류할 것 / 기각할 것 + 각각의 이유
- Codex가 내 가설을 기각했다면, 그 판단이 맞는지 코드로 재확인한 결과
- 실제 적용 계획
```

- Codex가 직접 쓴 파일이면 `author: codex`, `send.sh` 가 최종 메시지를 회수해 저장했으면 `author: codex-via-stdout`.
- `codex-via-stdout` 이면 **Codex 가 파일을 못 쓴 것이다.** 왜 못 썼는지 `.log/<스탬프>_stderr.log` 를 확인해라 — 응답 내용 자체가 "읽지 못했다"류의 실패 보고일 수 있다. 그 경우 분석이 아예 없는 것이니 검토할 것도 없다. **내용을 확인하지 않고 "응답 도착"만으로 성공 판정하지 마라.**
- **Codex 원문을 고치지 마라.** 검토는 반드시 아래에 새 섹션으로 덧붙인다. 섞으면 나중에 "Codex가 실제로 뭐라 했는지"를 잃는다.

## 품질 규칙

- **Claude의 가설 명시 필수** — 이게 빠지면 Codex가 다른 시선을 줄 수가 없다. 이 스킬의 존재 이유다.
- **시도-실패 이력 필수** — 없으면 이미 실패한 방법을 다시 제안받는다.
- **실제 코드 인용 필수** — 요청서 하나로 판단 가능해야 한다. 추측으로 코드를 지어내지 마라.
- **`response_path` 를 frontmatter에 박기 필수** — Codex에게 이름을 짓게 하면 짝이 깨지고, `send.sh` 가 이 값 없이는 동작하지 못한다.
- **쓰기 실패 폴백 문구 필수** — Codex가 저장에 실패해도 최종 메시지로 회수되도록. 이게 없으면 분석이 통째로 날아간다.
- **`run_in_background: true` 필수** — 동기로 부르면 Bash 타임아웃(최대 10분)에 걸리고, 대기 중 사용자가 다른 작업을 지시할 수 없다.
- **응답 "도착"과 응답 "성공"을 구분하라** — 파일이 생긴 것만으로 성공이 아니다. Codex가 요청서를 못 읽었다는 실패 보고가 담겨 있을 수 있다. 반드시 내용을 읽고 판정한다.
- **Codex의 무단 변경 보고는 검토보다 우선** — `send.sh` 가 🔴 로 보고하면 그것을 먼저 사용자에게 알린다. 임의로 되돌리지 않는다.
- **환경 구체적으로** — 버전·기기·툴이 원인인 경우가 많다(예: Compose Dialog 인셋, Android regex 플래그).
- **스탬프는 실측** — `date` 실행 결과만 쓴다.
- **Codex가 쓰는 파일은 response 하나뿐** — 그 외 쓰기는 시키지 않는다. 샌드박스 마찰을 최소면적으로 줄인다.

## 보안

- **PAT·API 키·토큰·비밀번호·개인 메모 본문**은 절대 넣지 않는다.
- 파일 경로·함수명·버전은 넣어도 된다. 비밀값이 코드에 있으면 `<REDACTED>` 로 마스킹.
- request 파일은 외부(Codex)로 나가고 **git에도 커밋된다** — 유출되면 안 되는 값은 원천 배제.

## 출력 형식

`send.sh` 를 던진 뒤 채팅에는 **짧게** 이것만 낸다. 사용자가 할 일이 없으므로 지시할 것도 없다.

1. **모드 한 줄** — 어느 모드로 갔는지. 맥락으로 판정했으므로 **반드시 밝힌다.**
   - `모드: 검토(Codex는 분석만)` / `모드: 코드리뷰(git diff, Codex는 분석만)` / `모드: 수정(Codex가 직접 고침)`
2. **무엇을 대상으로 했는지 한 줄**
   - CONSULT·EDIT → 어느 문제로 썼는지 (자동 특정했다면 반드시)
   - REVIEW → 리뷰 범위 (`커밋 안 된 변경` / `main 대비` / `커밋 abc123`)
3. 생성한 파일 경로 (요청서 또는 리뷰 결과가 생길 경로)
4. 한 줄: `Codex 에 넘겼습니다. 끝나면 제가 자동으로 받아 검토합니다 — 그동안 다른 작업 지시하셔도 됩니다.`

요청서 본문 전체를 채팅에 다시 뿌리지 않는다 — 파일이 정본이다.
검토가 끝난 뒤에는 채택/보류/기각과 적용 계획을 보고한다.

🔴 **모드를 밝히는 게 중요하다.** 키워드가 아니라 뜻으로 판정하므로 오판 가능성이 있다.
사용자가 한 줄만 보고 "그게 아니라 이거야" 라고 바로잡을 수 있어야 한다.

## 검증 상태 — 무엇이 실측됐고 무엇이 아닌가 (2026-08-17)

🔴 **이 표를 지우지 마라.** 미검증 기능을 검증된 것처럼 쓰지 않기 위한 것이다.
Codex 2차 검토에서 위험도 순위를 받아 반영했다.

| 기능 | 상태 |
|---|---|
| CONSULT 왕복 (전달·회수·자동 재호출) | ✅ 실측 8회+. **성공·실패 모두** 자동 인지 확인 |
| REVIEW — 자동 스코프(uncommitted/기본브랜치) | ✅ 실측. 심은 결함 4종을 5개로 탐지 |
| `-o` 폴백 · `stale` 판정 · `CR_SANDBOX=read-only` | ✅ 실측 |
| 변경 감지 · fail-closed | ✅ 실측 (감지 준비 실패 시 중단 확인) |
| 입력 검증 (git 레포·슬러그·frontmatter·response_path) | ✅ 실측 |
| **동시 실행** | ✅ **lock 으로 차단.** 2026-08-17 추가·실측 |
| **EDIT — 게이트 + 실제 수정** | ✅ **실측 완료 (2026-08-17).** 폐기용 git fixture 에서 7항목 검증: 게이트 차단 → 해금 통과 · 대상 파일 실제 수정 · **대상 외 파일 보호** · **커밋 안 한 작업 보존** · 감지가 **의도된 변경도** 잡음 · `git diff` 사후 확인 · Codex 가 "최소 변경" 지시를 지킴(1줄만, 무관한 리팩터 없음)<br>⚠️ 단 **버그가 명백한 1줄 수정** 케이스였다. **여러 파일이 얽힌 복잡한 변경은 미검증** |
| 🔴 REVIEW `--base` · `--commit` | ❌ DRYRUN 만. 실제 diff 범위와 리뷰 범위를 대조한 적 없다 |
| 🔴 서버에서 실제 Codex 왕복 | ❌ DRYRUN 만. 문법·Linux 분기·경로 변환만 확인했다 |
| RESUME · HELP | ❌ 미검증 (위험 낮음. 단 RESUME 이 edit 요청서를 재개하면 EDIT 위험을 상속한다) |
| 비용 상한 | ❌ 없음 — **의도적이다.** 사용자 판단(2026-08-17): 이 스킬 발동은 **하루 두세 번** 수준으로 빈도가 낮아 상한이 필요 없다. 근거 있는 숫자가 없어 임의값을 박지 않는다 |

이중 위임(서버 Claude 를 시켜 Codex 를 부르기)은 하지 않는다 — 위 § 이중 위임 참조.
단 **사용자가 서버에서 직접 쓰는 것은 정상이다.**

## 트러블슈팅 — 실측으로 확인된 것

**Codex 가 파일을 못 읽고 "요청서를 읽지 못했습니다" 로 답한다** — 🟢 **2026-08-17 수리 완료**

`.log/<스탬프>_stderr.log` 에 아래가 반복되면 이 케이스다.

```
windows sandbox: helper_sid_resolve_failed:
  resolve SID for offline user CodexSandboxOffline failed:
  LookupAccountNameW failed for CodexSandboxOffline: 1332
```

- `1332` = `ERROR_NONE_MAPPED`. Codex 가 권한 격리에 쓰는 **로컬 계정이 존재하지 않는다.**
- `[windows] sandbox = "elevated"` 는 `CodexSandboxOffline`/`CodexSandboxOnline` 두 로컬 계정으로
  격리한다. `~/.codex/.sandbox/setup_marker.json` 이 "셋업 완료"를 주장하면 Codex 는
  **계정을 재생성하지 않고 SID 조회만 시도**한다. 계정이 사라진 뒤에는 영구히 실패한다.
- 이 상태에서는 **`-s` 를 어떤 값으로 줘도 안 된다.** 읽기조차 샌드박스 헬퍼를 통과해야 하기 때문이다.

**수리 내역 (2026-08-17, 사용자 지시)**

1. 근본 복구(계정 재생성) 시도 → **실패.** 마커를 치워도 Codex 는 계정을 만들지 않는다.
   로컬 계정 생성에는 관리자 권한이 필요하고 현재 셸은 일반 사용자다
2. 전역 `~/.codex/config.toml` 을 **`sandbox = "unelevated"`** 로 변경 → 해결
   - 백업: `~/.codex/_backup_20260817_repair/` (config.toml · setup_marker.json · setup_error.json)
   - 되돌리려면 그 백업의 `config.toml` 을 복원한다
   - 마커는 "계정이 있다"는 **거짓 정보**이고 그것이 고장의 원인이었으므로 복원하지 않고 백업에만 뒀다
3. 검증: `-c` 오버라이드 없이 codex CLI 파일 읽기 성공, SID 에러 0건.
   OpenAI 공식 플러그인(`codex-plugin-cc`)도 명령 실패 4회 → 0회로 정상화

**서버 4대는 이 문제가 없다** — `.sandbox` 디렉토리 자체가 없다. `[windows]` 섹션은 Windows 전용이고
Linux 는 seccomp/landlock 으로 격리하므로 계정 기반 메커니즘을 쓰지 않는다.

재발하면(예: config 를 elevated 로 되돌린 경우) `send.sh` 의 `-c windows.sandbox=unelevated`
안전망이 이 스킬만은 살려둔다. 다른 Codex 작업까지 고치려면 위 2번을 다시 적용한다.

**응답이 `author: codex-via-stdout` 으로 저장됐다** — 배관은 정상이고 Codex 가 파일을 못 쓴 것이다.
위 샌드박스 문제이거나 경로 권한 문제다. 응답 내용부터 읽어 실패 보고인지 실제 분석인지 판정한다.

**응답이 `stale` 로 보고됐다** — 응답 파일 내용이 실행 전과 해시까지 동일하다. 즉 **이번 실행에서
Codex 가 아무것도 쓰지 못했고, 그 파일은 이전 실행의 결과물이다.** 이번 응답으로 오인하지 마라.
기존 파일을 임의로 덮어쓰거나 지우지 말고 사용자 판단을 받는다.

## 변경 감지의 한계 — 이것까지는 못 잡는다

Codex 의 2차 검토(2026-08-17)에서 확인된 것이다. "응답 파일 외 변경 없음" 은 **"감시 범위에서
최종 상태 차이가 없었다"** 는 뜻이며, 아래는 통과한다. 보고할 때 이 범위를 넘겨 단정하지 마라.

- **제외 영역 내부** — `.git`·`node_modules`·`build`·`.gradle`·`.dart_tool`·`.venv`·`.next`·`__pycache__`
  (성능을 위한 의도된 절충. `.git` 내부 변경도 안 잡힌다)
- **생성 후 삭제** — 실행 중 만들고 지우면 최종 상태에 흔적이 없다
- **기존 파일의 mtime 원복** — `find -newer` 는 mtime 만 본다. 내용을 바꾸고 mtime 을 되돌리면 안 잡힌다
  (잡으려면 전후 SHA256 manifest 가 필요하다. 이 루트만 6,000여 개 파일이라 비용 때문에 보류했다)
  ※ **새 파일**은 mtime 이 보존돼도 경로 목록 차집합(`comm -13`)으로 잡는다
- **삭제 후 같은 경로에 같은 mtime 으로 재생성** — 전후 경로가 같고 mtime 기준도 안 넘으면 놓친다
- **Codex 가 남긴 자식 프로세스의 지연 변경** — `codex exec` 가 반환한 뒤 백그라운드 자식이
  파일을 바꾸면 사후 1회 스캔에는 안 잡힌다
- **디렉토리·메타데이터** — 빈 디렉토리 생성, 권한/ACL, NTFS ADS, symlink/junction
- **줄바꿈이 포함된 파일명** — 줄 단위 처리라 한 파일이 여러 경로로 쪼개진다
- **원인 귀속 불가** — OneDrive 동기화나 다른 프로세스의 변경과 Codex 의 변경을 구별할 수 없다.
  변경이 잡히면 `.log/<스탬프>_events.jsonl` 에서 Codex 가 실제로 실행한 동작을 대조해라

위협 모델은 **"프롬프트를 안 지킨 Codex"** 다. mtime 을 일부러 되돌리는 능동적 은폐는 상정하지 않는다.
그 수준까지 막아야 하면 `CR_SANDBOX=read-only` 를 쓰는 것이 근본적이다 — Codex 에게 쓰기 권한을
아예 주지 않고, `-o` 로 받은 최종 메시지를 이 스크립트가 응답 파일로 저장한다. **코드 변경 없이
환경변수만으로 전환된다** (실측 검증됨).

## 인자 — 한글로 말하는 게 정상 사용법이다

사용자는 STT 로 말한다. 영문 플래그를 요구하지 마라. **뜻으로 알아듣는다.**

| 이렇게 말한다 | 간다 |
|---|---|
| `/codex_rescue` (인자 없음) | ★ **가장 흔함.** CONSULT. 막힌 걸 Claude 가 맥락에서 직접 특정 |
| `검토요청해` · `분석해줘` · `봐달라고 해` · `물어봐` | CONSULT |
| `리뷰시켜` · `리뷰해줘` · `바뀐 거 검토해` · `커밋 전에 훑어줘` | REVIEW |
| `리뷰시켜 인증 위주로` · `main 기준으로 리뷰해` | REVIEW + 집중지시/스코프 |
| `검토및 수정요청해` · `분석하고 수정시켜` · `수정시켜` · `고쳐줘` | EDIT (**확인 한 줄 받고** 진행) |
| `<request 파일 경로>` (인자 전체가 경로) | RESUME |
| `help` · `사용법` · `?` | HELP |
| 문제 텍스트 (`로그인이 안 돼`) | CONSULT + 그 문제에 집중 |

영문 플래그도 계속 받는다(`--readonly` · `--edit` · `--review`) — 하위 호환이지만 권장하지 않는다.

**서술형은 모드 지시가 아니다.** "리뷰가 안 돼" · "수정이 안 먹혀" 는 **문제 텍스트**다 → CONSULT.

## 파일

```
~/.claude/skills/codex_rescue/
  SKILL.md    이 문서
  send.sh     codex 호출 + 응답 회수 + 변경 실측. Claude 가 백그라운드로 던진다
```

`send.sh` 진입점은 두 개다.

```bash
# CONSULT / EDIT — 요청서 기반 (mode 는 frontmatter 가 정한다)
send.sh <request 파일 경로>

# REVIEW — git diff 기반. 요청서가 없다
send.sh --review --slug <슬러그> [--uncommitted|--base <브랜치>|--commit <SHA>] [집중지시]
```

환경변수는 양쪽 공통이다 — `CR_MODEL` · `CR_SANDBOX`(REVIEW 는 read-only 고정이라 무시) ·
`CR_WIN_SANDBOX` · `CR_ALLOW_EDIT` · `CR_DRYRUN`.
⛔ `CR_TIMEOUT` 은 제거됐다 — Windows 에서 작동하지 않아서다(실측). 쓰면 스크립트가 거부한다.

`send.sh` 를 고쳤으면 다른 머신에도 배포해야 한다 — `/skill_cp_install push codex_rescue`.
